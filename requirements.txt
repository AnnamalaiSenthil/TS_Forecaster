# ───────────────────────────────────────────────────────────
# Core Python libraries
streamlit>=1.23.1
numpy>=1.25.0
pandas>=2.0.0
matplotlib>=3.7.0

# Force a SciPy version with a prebuilt wheel, to avoid Fortran build errors
scipy==1.10.1

# PyTorch (CPU + CUDA variants are both supported; this will pull CPU by default)
torch>=2.0.0

# GluonTS (will pull in its dependencies, but because we pinned scipy, it won’t try to build from source)
gluonts>=0.12.0

# Hugging Face Hub (for hf_hub_download)
huggingface_hub>=0.18.0

# uni2ts (contains MoiraiForecast, MoiraiModule, etc.)
uni2ts>=0.1.0

# Chronos pipeline (BaseChronosPipeline)
chronos>=0.1.0
# ───────────────────────────────────────────────────────────
