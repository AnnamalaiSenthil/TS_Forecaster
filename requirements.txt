# ───────────────────────────────────────────────────────────
# Core data‑science / ML libraries
streamlit>=1.23.1
numpy>=1.25.0
pandas>=2.0.0
matplotlib>=3.10.3

# Use SciPy 1.12.X (prebuilt wheels for Python 3.13) to avoid a source build
scipy==1.12.0

# PyTorch (will pull the Linux wheel for your Python version)
torch>=2.0.0

# GluonTS (for PandasDataset, split, etc.)
gluonts>=0.12.0

# Hugging Face Hub (for hf_hub_download)
huggingface_hub>=0.18.0

# uni2ts (contains MoiraiForecast, MoiraiModule, etc.)
uni2ts>=0.1.0

# Chronos pipeline (BaseChronosPipeline)
chronos>=0.1.0
# ───────────────────────────────────────────────────────────
